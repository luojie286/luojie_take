# 苍穹外卖项目与开发环境总结

## 1. 项目概况

当前分析和配置的项目位于：

```text
E:\1、黑马程序员Java项目《苍穹外卖》企业级开发实战\资料\资料\day01\后端初始工程\sky-take-out
```

这是一个 Maven 多模块 Spring Boot 后端工程：

```text
sky-take-out
├─ sky-common   公共常量、异常、JWT、OSS、微信支付等工具
├─ sky-pojo     Entity、DTO、VO 等数据模型
├─ sky-server   Controller、Service、Mapper、鉴权和启动类
└─ pom.xml      父工程及统一依赖配置
```

初始工程包含了许多后续课程会使用的类，但目前真正实现的业务主要是员工登录和退出。学习时不需要一次性看懂所有文件，应先掌握下面这条基本调用链：

```text
请求 → Controller → Service → Mapper → MySQL → 返回 Result
```

## 2. 是否需要自己编写所有文件

实际开发不需要每次从零编写项目中的所有基础文件。

常见来源包括：

- Spring Initializr 等脚手架生成项目结构；
- 公司或团队提供统一响应、异常处理、鉴权等公共模板；
- 根据官方文档集成 OSS、支付和第三方 SDK；
- IDE 或代码生成器生成一部分重复代码。

现阶段应重点掌握：

- Java 基础、接口、异常和集合；
- Maven 项目结构和依赖；
- Spring Boot、依赖注入和配置文件；
- Controller 接收 HTTP/JSON 请求；
- Service 编写业务逻辑；
- MyBatis 操作数据库；
- Entity、DTO、VO 的区别；
- JWT 和拦截器的基本原理。

Redis、OSS、微信支付和 WebSocket 可以在课程进行到对应阶段时再学习。

## 3. JDK 配置结果

电脑当前安装了两套 JDK：

```text
JDK 17：C:\Program Files\Zulu\zulu-17
JDK 25：C:\Program Files\Zulu\zulu-25
```

两套 JDK 可以并存，不需要反复卸载和安装。

苍穹外卖项目目前使用 JDK 17，原因是它与 Spring Boot 2.7 和课程使用的旧依赖更加匹配，学习过程中更稳定。

当前用户的 `JAVA_HOME` 已设置为：

```text
C:\Program Files\Zulu\zulu-17
```

注意：修改 `JAVA_HOME` 后，需要重新打开终端或重启 VS Code 才会生效。

## 4. 项目中的兼容配置

根 `pom.xml` 已明确指定 Java 17：

```xml
<java.version>17</java.version>
<maven.compiler.release>17</maven.compiler.release>
<maven.compiler.source>17</maven.compiler.source>
<maven.compiler.target>17</maven.compiler.target>
```

Lombok 已调整为首个正式支持 JDK 17 的版本：

```xml
<lombok>1.18.22</lombok>
```

本机 Maven 的全局配置中存在一个默认启用的 `jdk-25-default` Profile，会强制所有项目使用 Java 25。为了不影响其他项目，苍穹外卖根目录中增加了：

```text
.mvn/maven.config
```

其内容为：

```text
-P!jdk-25-default
```

这只会为苍穹外卖项目禁用全局 Java 25 Profile，不会影响其他项目。

## 5. 构建验证

已使用 JDK 17 执行：

```powershell
mvn clean test
```

验证结果：

```text
sky-take-out  SUCCESS
sky-common    SUCCESS
sky-pojo      SUCCESS
sky-server    SUCCESS

BUILD SUCCESS
```

这说明三个子模块都可以在 JDK 17 下成功编译。目前项目没有自动化测试用例，因此该结果主要证明编译和 Maven 配置正常。

## 6. VS Code 配置

在 VS Code 中按 `Ctrl + Shift + P`，执行：

```text
Java: Configure Java Runtime
```

将项目运行时设置为：

```text
C:\Program Files\Zulu\zulu-17
```

如果 VS Code 仍显示旧版本或错误提示，再执行：

```text
Java: Clean Java Language Server Workspace
```

然后重启 Java 语言服务器或 VS Code。

### VS Code 中打开终端

可以使用以下任一方式：

- 按 `Ctrl + 反引号`；
- 选择菜单“终端 → 新建终端”；
- 按 `Ctrl + Shift + P`，执行 `Terminal: Create New Terminal`。

在新终端中使用以下命令检查环境：

```powershell
$env:JAVA_HOME
java -version
mvn -version
```

判断 Maven 使用哪个 JDK 时，应以 `mvn -version` 的输出为准。

## 7. VS Code 中出现过的提示

### 项目使用 Java 17，是否升级到最新 LTS

这是 VS Code 推荐安装 GitHub Copilot modernization 扩展的提示，不是错误。苍穹外卖课程项目应继续使用 Java 17，可以点击：

```text
Not Now
```

不要让扩展自动升级 Spring Boot、Java 和项目代码，以免偏离课程环境。

### Null annotation types 提示

这是可选的空值静态分析功能，与 JDK、Maven 和程序运行无关。课程初期可以点击：

```text
Disable
```

关闭它不会修改业务代码，也不会影响编译和运行。

## 8. 临时切换到 JDK 25

只对当前 PowerShell 窗口生效：

```powershell
$env:JAVA_HOME = "C:\Program Files\Zulu\zulu-25"
$env:Path = "$env:JAVA_HOME\bin;" + $env:Path
```

验证：

```powershell
java -version
mvn -version
```

关闭该终端后，临时设置失效。

如果要把用户默认 JDK 改回 25，可执行：

```powershell
setx JAVA_HOME "C:\Program Files\Zulu\zulu-25"
```

随后关闭并重新打开终端。苍穹外卖项目仍建议在 IDE 中单独选择 JDK 17。

## 9. Git 和个人项目目录建议

不要长期直接修改课程资料目录。推荐保留一份未经修改的课程原始工程，再将项目复制到自己的开发目录，例如：

```text
E:\JavaProjects\sky-take-out
```

推荐的工作方式：

```text
课程资料目录：保留原始版本，用于对照
个人项目目录：实际开发、练习和提交 Git
```

在个人项目根目录中初始化 Git：

```powershell
git init
git add .
git commit -m "初始化苍穹外卖后端工程"
```

`.gitignore` 至少应包含：

```gitignore
target/
.idea/
.vscode/
*.iml
*.log
```

JDK 本身不应复制到项目目录，也不应提交到 Git。应该提交的是 `pom.xml`、`.mvn/maven.config`、源代码和必要的项目配置。

数据库真实密码、JWT 密钥、OSS 密钥等敏感信息不能提交到公开仓库，应通过环境变量或本地配置提供。

## 10. 推荐学习顺序

```text
先运行项目
→ 理解登录调用链
→ 完成简单 CRUD
→ 理解 DTO、Entity、VO
→ 学习 JWT 和拦截器
→ 再学习 Redis、OSS、支付和 WebSocket
```

不需要一开始就掌握所有工具类和依赖。先能独立完成一条简单业务链路，再逐步扩展，是更有效的学习方式。
